import Form from "components/Form";
import React from "react";
import { GlobalAdsWrapper, Title } from "./styles";

const GlobalAds = () => {
  return (
    <GlobalAdsWrapper>
      <Title>Информация</Title>
      <Form />
    </GlobalAdsWrapper>
  );
};

export default GlobalAds;
