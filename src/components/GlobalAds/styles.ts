import styled from "styled-components";
import { H2 } from "ui/typography";

const GlobalAdsWrapper = styled.div``;

const Title = styled(H2)`
  margin-bottom: 40px;
`;

export { GlobalAdsWrapper, Title };
